{
  "private": true,
  "name": "notion-backups",
  "version": "1.0.0",
  "description": "A template for kick starting a Cloudflare Workers project",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "yudax42 <yudax42@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "html-loader": "^1.3.2",
    "prettier": "^1.18.2"
  }
}
